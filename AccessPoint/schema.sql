/* (Beta) Export of data model AccessPoint of the subject dataModel.WifiNetwork for a postgreSQL database. Pending translation of enumerations and multityped attributes */
CREATE TYPE apState_type AS ENUM ('up', 'down');CREATE TYPE AccessPoint_type AS ENUM ('AccessPoint');
CREATE TABLE AccessPoint (address json, alternateName text, apState apState_type, areaServed text, category json, clientsConnected text, controlledProperty json, dataProvider text, dateCreated timestamp, dateInstalled timestamp, dateLastReboot timestamp, dateLastValueReported timestamp, dateModified timestamp, description text, email text, firmwareVersion text, hardwareVersion text, id text, ipAddress text, location json, macAddress text, modelName text, name text, osVersion text, owner json, refPointOfInterest text, seeAlso json, serialNumber text, service json, softwareVersion text, source text, ssid json, supportedProtocol json, timeInstant timestamp, type AccessPoint_type);